# APK downloads

Coloca aquí los archivos APK con el nombre:

```
jnob-fit-{version}-beta.apk
```

Ejemplo: `jnob-fit-1.2.8-beta.apk`

Después actualiza `data/releases.json` (campo `latest` y entrada en `releases`).

No edites los enlaces en `index.html` — se generan automáticamente desde el JSON.
